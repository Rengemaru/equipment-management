# CLAUDE.md

サークル備品管理システムの開発ガイド。作業前に必ず読むこと。

---

## プロジェクト概要

大学サークルの備品（本・撮影機材・工具など）を管理するWebアプリ。
「今どこに何があり、誰が持っているか」を継続的に把握できる状態を作ることが目的。

**利用者は数十名の学生。主にスマートフォンから使う。オンプレミスで運用する。**

---

## 最重要：開発順序を厳守する

**M1 → M2 → M3 → M4 の順に、1つずつ完成させてから次に進む。**

| | 内容 | 完了条件 |
|---|---|---|
| **M1** | 認証・権限・備品マスタ・CSVインポート・QR発行 | 棚卸しCSVを取り込み、QRラベルを印刷して貼れる |
| **M2** | 貸出・返却・破損報告・一覧 | QRを読んでから3タップ以内で借用が完了する |
| **M3** | 所在不明報告・定期通知・棚卸しモード | 通知が重複せず届き、連続スキャンで棚卸しできる |
| **M4** | 公開API・サイネージ連携 | 個人情報の混入なくサイネージに表示できる |

### 守るべきこと

- **先のマイルストーンの機能を先取りして実装しない。** M1で貸出ボタンを置かない、M2で通知を送らない
- **押せないボタンやダミー画面を置かない。** 混乱のもとになる
- **M4は必ず最後。** データ構造が固まる前にAPIを公開すると、サイネージ側を壊さずに変更できなくなる
- 各マイルストーンの受け入れ条件は `docs/m{N}-implementation-spec.md` にある。**全て満たしてから次へ進む**
- M3以降の仕様は M1・M2 の運用後に見直す前提。**実装前に仕様書の妥当性を確認する**

---

## 設計思想（実装で迷ったらここに戻る）

1. **記録する手間 < 記録しない手間。** この不等号が崩れた機能は使われない。確認ダイアログ、必須入力、ログイン要求を安易に増やさない
2. **借用の記録漏れは致命的、返却の記録漏れは自己修復する。** 実装の作り込みは借用側に厚く配分する
3. **追跡対象を減らすことが遵守率を上げる最短経路。** 自由利用品（`is_free_use`）は貸出フローから完全に除外する
4. **記録されなかった事実を「正常」として表示しない。** 不確かな状態は不確かなまま表現する（`所在不明_未確認` など）
5. **可視性は罰則より強い。** 誰が何を持っているかが全員に見える状態を作る

### 承認フローを作らない

所在不明報告も破損報告も、**メンバーの報告は即時反映する。** 運営は事後に追認する。

承認待ちの間、システムが「棚にある」と表示し続ける状態は、紙の台帳より悪い。記録が正しいという嘘を、システムが権威をもって主張することになる。

---

## 技術スタック

| 領域 | 選定 |
|---|---|
| バックエンド | Go 1.22+ |
| ルーティング | 標準 `net/http` の `http.ServeMux`（フレームワークを入れない） |
| DB | SQLite（`modernc.org/sqlite`。cgo不要のため） |
| マイグレーション | `embed` + 連番SQL の自作ランナー |
| フロントエンド | React + TypeScript + Vite + React Router + Tailwind |
| QR生成 | `github.com/skip2/go-qrcode` |
| PDF生成 | `github.com/go-pdf/fpdf` |
| メール | 標準 `net/smtp` |
| コンテナ | Docker（マルチステージ） |

### 依存を増やさない

このプロジェクトは**数年で担当者が全入れ替えする。** 引き継いだ人が読めることが、コードの短さより優先される。

- 標準ライブラリで書けるものは標準ライブラリで書く
- ライブラリを追加したくなったら、まず標準ライブラリで書けないか検討する
- ORM を導入しない。`database/sql` と手書きSQLを使う

### 単一バイナリ構成

フロントエンドのビルド成果物を Go バイナリに `embed` する。**デプロイは「バイナリ1つ + SQLiteファイル1つ」で完結させる。**

---

## ディレクトリ構成

```
.
├── CLAUDE.md
├── docs/                      # 仕様書（変更時はここも更新する）
│   ├── requirements.md
│   ├── url-design.md
│   └── m1〜m4-implementation-spec.md
├── cmd/server/main.go
├── internal/
│   ├── auth/                  # マジックリンク・セッション
│   ├── item/                  # 備品マスタ
│   ├── loan/                  # 貸出（M2）
│   ├── report/                # 所在不明・破損報告（M2/M3）
│   ├── notify/                # 通知（M3）
│   ├── inventory/             # 棚卸し（M3）
│   ├── label/                 # QR・ラベルPDF
│   ├── publicapi/             # 公開API（M4）
│   ├── httpx/                 # ルーティング・ミドルウェア
│   └── db/
│       ├── db.go
│       └── migrations/        # 0001_init.sql, 0002_....sql
├── web/                       # React + Vite
├── assets/fonts/              # NotoSansJP.ttf（embed する）
├── schema.sql                 # 現行スキーマの参照用
├── Dockerfile
└── Makefile
```

---

## コマンド（macOS）

```bash
# 依存取得
go mod download
cd web && npm install && cd ..

# 開発（バックエンド）
make dev-api          # go run ./cmd/server

# 開発（フロントエンド）
make dev-web          # cd web && npm run dev

# ビルド（フロントをembedして単一バイナリ）
make build

# テスト
make test             # go test ./...
make test-web         # cd web && npm test

# DBの中身を見る
sqlite3 data/app.db    # 未インストールなら brew install sqlite

# Dockerビルド・起動
make docker-build
make docker-up
```

`Makefile` が未整備なら、まず作ること。**コマンドを覚えさせない。**

---

## DB の扱い

### 絶対に守ること

- **`items` に「貸出中フラグ」を持たせない。** 貸出状態は `loans` から導出する（`returned_at IS NULL`）。二重管理は必ず不整合を起こす
- **`loans` の行を削除しない。** 返却しても残す。破損・紛失の追跡はこの履歴が唯一の根拠
- **`users` を削除しない。** 卒業者は `is_active = 0`。削除すると貸出履歴が壊れる
- **`items` を物理削除しない。** `condition = '廃棄'` で表現する
- **備品コードは人手で振らせない。** システムが `0001` 形式で自動採番する。抜け・重複が必ず起きるため

### スキーマ変更

`internal/db/migrations/` に連番SQLを追加する。既存ファイルを書き換えない。
変更したら `schema.sql` と該当する仕様書も更新すること。

### バックアップ

WALモードで動くため、単純なファイルコピーは危険。`.backup` コマンドか、書き込み停止中のコピーを使う。

---

## 実装上の落とし穴

### 認証後のリダイレクト（最重要）

`/login?next=/i/0042` の `next` による復帰を必ず実装する。**QRから来た未ログインユーザーを認証後にトップへ放り出すと、もう一度QRを読み直させることになる。この一手間が記録漏れの直接原因になる。**

`next` は自サイト内の相対パスのみ許可（オープンリダイレクト対策）。

### PDFの日本語フォント

ラベルに品名を印字するため、日本語フォントの埋め込みが必須。**Noto Sans JP の TTF をバイナリに `embed` して `fpdf` に登録する。** Dockerイメージにフォントを置く方式は、イメージ差し替えで壊れるので使わない。

### 権限チェックはAPI側で行う

UIでボタンを隠すだけにしない。**member が備品マスタを書き換えられないことが、スプレッドシート共有ではなくシステム化する主目的。** ハンドラごとにロールを検証する。

### 通知の冪等性

アプリ内スケジューラは再起動で実行タイミングを取りこぼす。**`notification_log` の `(user_id, kind, dedupe_key)` ユニーク制約で判定し、起動時にも未送信分を送る。** 時刻トリガーだけに依存しない。

### 公開APIにビューを直接返さない

DTO を介して変換する。スキーマ変更が即 API の破壊にならないようにする。
**`overdue` 以外のレスポンスに個人名を含めない。テストで明示的に検証する。**

---

## コーディング規約

- Go: `gofmt` 準拠。エラーは握りつぶさず必ず返す。`context.Context` を第一引数に取る
- SQL: 手書き。プレースホルダを使い、文字列結合でクエリを組み立てない
- TypeScript: `any` を使わない。API のレスポンス型を明示的に定義する
- UI: **モバイルファースト。** スマホ前提で設計し、レスポンシブで広げる
- コメント: 「なぜそうしたか」を書く。「何をしているか」はコードで表現する

---

## やらないこと

- 予約機能（スコープ外）
- 購入・会計管理（スコープ外）
- 書き込み系の公開API
- 記録漏れをゼロにする試み（物理的強制がない以上不可能。検知と事後回収で対処する）
- 凝ったアーキテクチャ。**引き継ぐ人が読めない構成は、このプロジェクトでは負債**

---

## 参照ドキュメント

| ファイル | 内容 |
|---|---|
| `docs/requirements.md` | 要件定義・データモデル・非機能要件 |
| `docs/url-design.md` | URL設計・QRの仕様・画面一覧 |
| `docs/m1-implementation-spec.md` | M1 詳細仕様と受け入れ条件 |
| `docs/m2-implementation-spec.md` | M2 詳細仕様と受け入れ条件 |
| `docs/m3-implementation-spec.md` | M3 詳細仕様と受け入れ条件 |
| `docs/m4-implementation-spec.md` | M4 詳細仕様と受け入れ条件 |
| `schema.sql` | 現行スキーマ（参照用） |

仕様と実装が食い違った場合、**勝手に実装を優先せず、どちらが正しいか確認する。**
