apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Keep the existing LAN/Tailscale ingress as a fallback route.
  - ../tailnet
  - cloudflared-deployment.yaml
  - cloudflared-pdb.yaml
